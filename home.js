function adjustFontSize(body, 1.2) {
  var fontSize = parseFloat(getComputedStyle(element).fontSize);
  element.style.fontSize = (fontSize * factor) + 'px';
}
function adjustImageDefinition(a href, 2) {
  var width = image.width;
  var height = image.height;
  image.style.width = (width * factor) + 'px';
  image.style.height = (height * factor) + 'px';
}
function reorderElements(header, h1 img, h1 strong em) {
  parent.insertBefore(child2, child1);
}
